# 🚀 Инструкция по добавлению проекта на GitHub

## Шаг 1: Создание репозитория на GitHub

1. Перейдите на [GitHub.com](https://github.com)
2. Нажмите кнопку **"New repository"** (зеленая кнопка)
3. Заполните данные:
   - **Repository name**: `niks-photography-website`
   - **Description**: `Современный сайт-портфолио фотографа с PWA функциональностью`
   - Выберите **Public** (для GitHub Pages)
   - ✅ Отметьте **"Add a README file"** (НЕ отмечайте, у нас уже есть)
   - ✅ Отметьте **"Add .gitignore"** → выберите **"Node"** (НЕ отмечайте, у нас уже есть)
   - ✅ Отметьте **"Choose a license"** → выберите **"MIT"** (НЕ отмечайте, у нас уже есть)

4. Нажмите **"Create repository"**

## Шаг 2: Инициализация Git в проекте

Откройте терминал в папке проекта и выполните:

```bash
# Перейдите в папку проекта
cd "c:\Users\makar\Downloads\Сайт никса\niks-artphoto.ru"

# Инициализируйте Git
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "🎉 Initial commit: Современный сайт фотографа с PWA"

# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/niks-photography-website.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

## Шаг 3: Настройка GitHub Pages

1. Перейдите в настройки репозитория: **Settings** → **Pages**
2. В разделе **Source** выберите:
   - **Deploy from a branch**
   - **Branch**: `main`
   - **Folder**: `/ (root)`
3. Нажмите **Save**
4. Сайт будет доступен по адресу: `https://YOUR_USERNAME.github.io/niks-photography-website/`

## Шаг 4: Настройка кастомного домена (опционально)

Если у вас есть домен:

1. Создайте файл `CNAME` в корне проекта:
   ```bash
   echo "niks-artphoto.ru" > CNAME
   git add CNAME
   git commit -m "Add custom domain"
   git push
   ```

2. В настройках DNS домена добавьте CNAME запись:
   ```
   www.niks-artphoto.ru → YOUR_USERNAME.github.io
   ```

## Шаг 5: Проверка деплоя

1. Дождитесь завершения GitHub Actions (вкладка **Actions**)
2. Проверьте сайт по ссылке GitHub Pages
3. Убедитесь, что PWA функции работают
4. Проверьте в Lighthouse (должно быть 100/100 для PWA)

## 🔧 Полезные команды Git

```bash
# Проверить статус
git status

# Добавить изменения
git add .

# Сделать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push

# Посмотреть историю
git log --oneline

# Создать новую ветку
git checkout -b feature/new-feature

# Переключиться на main
git checkout main

# Слить ветку
git merge feature/new-feature
```

## 📱 Проверка PWA

После деплоя проверьте:

1. **Lighthouse**: Откройте DevTools → Lighthouse → Generate report
2. **Установка**: В браузере должна появиться кнопка "Установить приложение"
3. **Офлайн**: Отключите интернет и проверьте работу сайта
4. **Service Worker**: DevTools → Application → Service Workers

## 🚨 Возможные проблемы

### HTTPS требуется для PWA
- GitHub Pages автоматически предоставляет HTTPS
- Для кастомного домена настройте SSL сертификат

### Service Worker не работает
- Убедитесь, что сайт открыт по HTTPS
- Проверьте консоль на ошибки
- Очистите кэш браузера

### Изображения не загружаются
- Проверьте пути к файлам (должны быть относительными)
- Убедитесь, что файлы добавлены в Git

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте вкладку **Actions** в GitHub на ошибки
2. Создайте **Issue** в репозитории
3. Напишите в Telegram: [@objektivniksa](https://t.me/objektivniksa)

## 🎉 Готово!

Ваш современный сайт фотографа теперь на GitHub и доступен всему миру! 

**Следующие шаги:**
- Настройте Google Analytics
- Добавьте больше фотографий в портфолио
- Оптимизируйте SEO для поисковых систем
- Поделитесь ссылкой в социальных сетях